{
    "type": "postgres",
    "url": "postgresql://user:user@localhost:5433/user?sslmode=disable",
    "synchronize": false,
    "logging": true,
    "migrationsRun": false,
    "entities": ["data/entity/**/*.ts"],
    "migrations": ["data/migrations/**/*.ts"],
    "cli": {
        "entitiesDir": "data/entity",
        "migrationsDir": "data/migrations"
    }
}