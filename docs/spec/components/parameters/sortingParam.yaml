in: query
name: 'page[order]'
required: false
schema:
  type: string
  enum:
    - ASC
    - DESC
  default: DESC
  description: >-
    Order of records on the page. If sortingParam is not specified, order of
    records is by default sorted by ID.
